import React from 'react';
import {withRouter} from 'react-router-dom';

const Design = () => {
  return (
    <div>
      <h1>Taller diseño</h1>
    </div>
  );
};

export default withRouter(Design);
